import React,{useEffect,useMemo,useState} from "react";import PairingScreen from "./pairing/PairingScreen";import LaptopDashboard from "./laptop/LaptopDashboard";import MobileDashboard from "./mobile/MobileDashboard";import {SignedSocket} from "./security/signedSocket";
export default function App(){const [session,setSession]=useState(null);const [view,setView]=useState("laptop");const [error,setError]=useState("");const socket=useMemo(()=>session?new SignedSocket({origin:session.origin,sessionId:session.sessionId,sessionKey:session.sessionKey,onSecurityError:e=>setError(e.message)}):null,[session]);useEffect(()=>{socket?.connect();return()=>socket?.disconnect();},[socket]);if(!session)return <PairingScreen onPaired={setSession}/>;const unpair=()=>{socket.disconnect();setSession(null);history.replaceState({},"",location.pathname);};return <><nav className="view-switcher"><button onClick={()=>setView("laptop")}>Laptop</button><button onClick={()=>setView("mobile")}>Mobile</button></nav>{error&&<div className="global-error">{error}</div>}{view==="mobile"?<MobileDashboard session={session} socket={socket}/>:<LaptopDashboard session={session} socket={socket} onUnpair={unpair}/>}</>}