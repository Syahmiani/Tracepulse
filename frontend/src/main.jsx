import React from "react";import {createRoot} from "react-dom/client";import App from "./App";import "./styles.css";const root=document.getElementById("root");if(!root)throw Error("root missing");createRoot(root).render(<React.StrictMode><App/></React.StrictMode>);if("serviceWorker" in navigator)addEventListener("load",async()=>{for(const registration of await navigator.serviceWorker.getRegistrations())await registration.unregister();if(window.caches)for(const key of await caches.keys())if(key.startsWith("tracepulse-"))await caches.delete(key);});